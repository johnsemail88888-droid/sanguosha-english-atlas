// 吴 Wu ability implementations (wave 2). Register each AbilityImpl with
// registerAbility({ id: '<AbilityDef.id>', ... }) — see shu.ts (关羽) for the
// reference pattern and common.ts for shared helpers.
export {};
