// STUB (orchestrator) — RENDER replaces with an offscreen three.js render of the hero model.
/** Render a hero portrait (bust, dramatic light) and return a data URL (PNG/WebP). Cached per id+size. */
export async function renderHeroPortrait(heroId: string, size = 256): Promise<string> {
  void heroId;
  void size;
  return 'data:image/gif;base64,R0lGODlhAQABAAAAACw=';
}
