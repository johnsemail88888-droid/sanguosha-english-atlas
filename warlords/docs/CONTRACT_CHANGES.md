# Contract changes log

Append one line per change to a frozen contract file (src/core/*, src/data/types.ts, src/sim/api.ts,
src/sim/host.ts, src/render/view.ts, src/game/session.ts): `- <file>: <what> (<who/why>)`.

- src/core/types.ts: PrivateHeroView gains optional `vel?`, `onGround?`, `moveMods?: PredictionMoveMods` (+ new exported type `PredictionMoveMods`) so clients can predict the local hero exactly; SIM-CORE please fill them in snapshotFor() for the receiving hero (NET; absent ⇒ client estimates)
- src/data/types.ts: HeroVisual.mount?: 'horse' | 'redHare' — hero is permanently drawn mounted (马超 马术, 吕布 赤兔); purely visual, speed comes from the passive (DATA)
- src/data/types.ts: HeroDef.quotesEn?: string[] — English translations of quotesZh, same order, for subtitles / en UI (DATA)
- src/data/types.ts: AbilityDef.dtype?: DamageType and ItemDef.dtype?: DamageType — the damage type of every hit the ability/item deals (present iff it deals damage). Ability/item damage is NOT a weapon hit (no weaponId) unless the ability has params.weaponHit = 1 (DATA; see data/heroes.ts header "Damage types")
- src/data/types.ts (convention, no type change): a 'lord'-slot AbilityDef WITHOUT cooldown is a passive lord skill (袁绍 血裔) — no activate(), G does nothing; UI/bots/world use `isPassiveAbility(a)` from src/data to skip it (DATA)
- src/core/types.ts (status-param convention, no type change) — SIM-CORE + NET please honour: `reveal` with params.viewerId = private reveal, only that viewer's snapshot gets the outline/minimap marker and only that viewer's canSee() is affected (诸葛亮 观星, 司马懿 狼顾 via params.privateReveal); `drunk` with params.weaponOnly = 1 (酒: ITEM_BY_ID.jiu.params.weaponOnly) is applied and consumed only by hits that carry a weaponId — items/basic.ts should forward weaponOnly and combat.ts step 3 should check req.weaponId (DATA)
- src/data/items.ts: new export BULLET_EVASION_CAP = 0.5 — SIM-CORE combat: combine 八卦 chance, dodgeChance statuses and 甄姬 倾国 as 1 − Π(1 − p) and cap the total evade chance at this value (DATA)
- src/core/types.ts: PrivateHeroView gains optional `forced?: { vel: Vec3; remaining: number }` (the hero's active Entity.forced velocity + seconds left) so clients replay dashes/knockbacks in prediction instead of rubber-banding; SIM-CORE please fill it in snapshotFor() for the receiving hero while `now < e.forced.until` (NET; absent ⇒ client blends the correction)
- src/core/types.ts: GameEvent = EventRouting & (…union…) — new exported interface EventRouting { privateTo?: EntityId }: an event carrying hidden information is delivered by NET only to the player of that hero (net/eventFilter.ts). SIM-CORE please set privateTo = params.viewerId on the 'status' events of a private 'reveal' (观星/狼顾), and on any future private event; NET additionally always treats {t:'reward', kind:'bounty'} as private to `who` (NET)
- src/core/types.ts: new export VF_EXPOSED = 1 << 26 (same bit sim/snapshot.ts already sets for the 'reveal' status, incl. per-viewer private reveals) so UI/render can read it without importing sim; SIM may re-export/import it from core instead of its local copy (UI)
